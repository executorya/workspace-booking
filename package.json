{
  "name": "workspace-booking",
  "version": "1.0.0",
  "private": true,
  "description": "Platform for booking workspaces and rooms.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build -w server && npm run build -w client",
    "db:seed": "npm run db:seed -w server"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
